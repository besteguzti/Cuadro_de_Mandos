# Frontend

Interfaz React del cuadro de mandos Aruba.

## Comandos

```powershell
npm install
npm run dev
npm run build
```

El frontend espera que el backend este disponible en `http://localhost:8080`.
