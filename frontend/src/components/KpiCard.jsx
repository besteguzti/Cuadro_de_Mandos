function KpiCard({ title, value }) {

    return (

        <article className="kpi-card">

            <h2>{title}</h2>

            <p>{value}</p>

        </article>
    )
}

export default KpiCard
